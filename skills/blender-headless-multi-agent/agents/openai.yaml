interface:
  display_name: "Blender Headless Multi-agent"
  short_description: "Run parallel Blender MCP workers without conflicts"
  default_prompt: "Use $blender-headless-multi-agent to automatically start headless Blender workers, build models, and clean them up safely."
